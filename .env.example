SUPABASE_URL=
SUPABASE_PUBLISHABLE_KEY=
SUPABASE_SERVICE_ROLE_KEY=
# Shared secret required by the Nextdoor extension webhook when WEBHOOK_SECRET is set in production.
WEBHOOK_SECRET=
VITE_SUPABASE_PROJECT_ID=
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# Crisp Integration Architecture
# Crisp workspace Website Tokens are managed through the JellyBean Admin UI.
# Each workspace is stored independently in Supabase Vault and linked through crisp_workspaces.
# Do not add per-site CRISP_* environment variables.
